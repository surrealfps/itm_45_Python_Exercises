Exercises were made in Jupyter Lab

2 sets of files are provided:
    1. Source Code done in Jupyter Lab (This should be opened with Jupyter Lab, if in any case the code cannot run in VS Code)
    2. Template with Source Code embedded inside
    
If there may be issues regarding the code (i.e. failure to run, error messages, etc.) please leave a comment on Canvas to indicate if I should reupload the code, provide a video of the code running or to run the code in person.